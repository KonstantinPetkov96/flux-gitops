apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: flux-guestbook
  namespace: flux-system
spec:
  interval: 1m
  chart:
    spec:
      driftDetection:
        mode: enabled
      chart: ./charts/helm-guestbook
      sourceRef:
        kind: GitRepository
        name: flux-repo
      version: 0.1.2
